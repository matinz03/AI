# Flowboard Kanban

A focused, local-only Kanban board built with Next.js.

```bash
cd frontend
npm install
npm run dev
```

Useful checks: `npm run lint`, `npm test`, `npm run build`, and `npm run test:e2e`.
